import Stats from "@/components/Stats";

export const metadata = {
  title: "Revenue — x402 Bazaar",
  robots: { index: false, follow: false },
};

export default function StatsPage() {
  return <Stats />;
}
